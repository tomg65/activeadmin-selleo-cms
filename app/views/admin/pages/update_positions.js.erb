<% if ActiveadminSelleoCms::Page.update_positions(params[:page_ids]) %>
    $('#wrapper').effect('highlight');
<% else %>
    alert('An error has occured. Changes are NOT saved');
<% end %>
$('.update-positions-button').attr('disabled', false).attr('value', 'Save')
